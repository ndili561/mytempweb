@using CRM.Entity.Model
@model CRM.Entity.Search.Employee.UserSearchModel
@{
    ViewData["User"] = "User List";
}
<form asp-action="Grid" method="get">
    <div class="row">
        <div class="panel">
            <div class="panel-body">
                <div class="row">
                    <div class="ibox float-e-margins">
                        <div class="ibox-title">
                            <div id="filterDiv" class="row">
                                <div class="col-md-12">
                                    <search />
                                </div>
                            </div>
                        </div>
                        <div id="@Model.TargetGridId" class="grid">
                            @Html.Partial("Grid", Model)
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</form>
@Html.Partial("ModalDialog", new ModalDialogViewModel { ParentDivId = "divUserParent" })

@section Scripts {
    <script type="text/javascript">
        function displayEditUser(id) {
            var url = "/User/Edit?id=" + id;
            showDialog("divUserParent", "frmUser", url, "Edit User");
        }
    </script>
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}

