@using CRM.Entity.Model
@using CRM.Entity.Search.Lookup
@model TaskTypeSearchModel
@{
    ViewData["Title"] = "Task Type List";
}
<form asp-action="Grid" method="get">
    <div class="row">
        <div class="panel">
            <div class="panel-body">
                <div class="row">
                    <div class="ibox float-e-margins">
                        <div class="ibox-title">
                            <div id="filterDiv" class="row">
                                <div class="col-md-12">
                                    <search />
                                </div>
                            </div>
                        </div>
                        <div id="@Model.TargetGridId" class="grid">
                            @Html.Partial("Grid", Model)
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</form>
@Html.Partial("ModalDialog", new ModalDialogViewModel { ParentDivId = "divTaskTypeParent" })

@section Scripts {
    <script type="text/javascript">
        function displayEditTaskType(id) {
            var url = "/TaskType/Edit?id=" + id;
            showDialog("divTaskTypeParent", "frmTaskType", url, "Edit Address Type");
        }
    </script>
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}

