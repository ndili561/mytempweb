@using CRM.Entity.Model
@model CRM.Entity.Search.Customer.PersonAlertCommentSearchModel
@{
    ViewData["Title"] = "Person Alert Comment List";
}
<form asp-action="Grid" method="get">
    <div class="row">
        <div class="panel">
            <div class="panel-body">
                <div class="row">
                    <div class="ibox float-e-margins">
                        <div class="ibox-title">
                            <div id="filterDiv" class="row">
                                <div class="col-md-12">
                                    <search />
                                </div>
                            </div>
                        </div>
                        <div id="@Model.TargetGridId" class="grid">
                            @Html.Partial("Grid", Model)
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</form>
@Html.Partial("ModalDialog", new ModalDialogViewModel { ParentDivId = "divPersonAlertCommentParent" })

@section Scripts {
    <script type="text/javascript">
        function displayEditPersonAlertComment(alertGroupId) {
            var url = "/PersonAlertComment/Edit?id=" + alertGroupId;
            showDialog("divPersonAlertCommentParent", "frmPersonAlertComment", url, "Edit Flag Group");
        }
    </script>
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}

